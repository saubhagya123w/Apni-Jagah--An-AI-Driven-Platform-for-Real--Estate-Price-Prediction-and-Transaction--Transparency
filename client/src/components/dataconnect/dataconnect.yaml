specVersion: "v1beta"
serviceId: "components"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "components-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
